<form method="post" action="{{ path('app_reservation_h_delete', {'id': reservation_h.id_rh}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation_h.id_rh) }}">
    <button class="btn">Delete</button>
</form>
