{% extends 'base2.html.twig' %}

{% block title %}Voituresss{% endblock %}

{% block body %}
    <p class="text-muted font-14 m-b-20">
    <table class="table">

<td>

    
    <h1>Voiture</h1>

    <table class="table">
        <tbody>
           
            <tr>
                <th>Matricule</th>
                <td>{{ voiture.matricule }}</td>
            </tr>
            <tr>
                <th>Marque</th>
                <td>{{ voiture.marque }}</td>
            </tr>
            <tr>
                <th>Couleur</th>
                <td>{{ voiture.couleur }}</td>
            </tr>
            <tr>
                <th>Image</th>
               <td> <img src="{{ asset('uploads/images/') ~voiture.image }}"
                    alt="" style="width: 600px; height: 400px"></td>
            </tr>
        </tbody>
    </table>

    

    <a href="{{ path('voiture_edit', {'idVoiture': voiture.idVoiture}) }}">Modifier </a>

    {% endblock %}
