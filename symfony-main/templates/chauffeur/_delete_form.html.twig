<form method="post" action="{{ path('chauffeur_delete', {'idChauffeur': chauffeur.idChauffeur}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chauffeur.idChauffeur) }}">
    <button class="btn-danger btn-rounded w-md waves-effect waves-light m-b-5">Supprimer</button>
</form>
