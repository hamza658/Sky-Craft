<form method="post" action="{{ path('voiture_delete', {'idVoiture': voiture.idVoiture}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ voiture.idVoiture) }}">
    <button class="btn">Delete</button>
</form>
